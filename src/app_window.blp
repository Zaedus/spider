using Gtk 4.0;
using Adw 1;
using WebKit 6.0;

template $AppWindow : Adw.ApplicationWindow {
  default-width: 500;
  default-height: 500;
  Adw.ToolbarView toolbar {
    [top]
    Adw.HeaderBar {}
  }
}
